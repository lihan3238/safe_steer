# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.ipynb_checkpoints/

# Secrets — never commit (keep .env.example only)
.env
!.env.example

# Data & model artifacts (rebuild via scripts/prepare_data.py)
data/*
!data/manifest.json
vectors/
*.pt
*.safetensors

# OS / editor
.DS_Store

# extracted activations (rebuild via scripts/extract_activations.py)
activations/

# eval outputs (rebuild via scripts/eval_steering.py)
eval/
